interface:
  display_name: "Backtest Workbench Ops"
  short_description: "Start and verify workbench pages"
  default_prompt: "Use $backtest-workbench-ops to start the chart workbench, connect it to a data-source API, and verify K-line, strategy result, instrument manager, schedule editor, and instrument sync pages."
