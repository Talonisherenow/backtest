interface:
  display_name: "Backtest IM Agent API"
  short_description: "Discover and use backtest HTTP APIs from IM"
  default_prompt: "Use $backtest-im-agent-api to answer IM data, crawler, task, and K-line requests through the backtest data-source HTTP API only; do not run local crawler scripts or server operations."
